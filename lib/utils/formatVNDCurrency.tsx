// Function to format VND currency
export const formatVNDCurrencyValue = (price: number) => {
  return new Intl.NumberFormat("vi-VN", {
    style: "currency",
    currency: "VND",
  }).format(price);
};
